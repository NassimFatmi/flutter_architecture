import 'package:flutter/material.dart';

class GacelaAppBar extends StatelessWidget {
  const GacelaAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar();
  }
}
